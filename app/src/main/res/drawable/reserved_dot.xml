<?xml version="1.0" encoding="utf-8"?>
<!-- One of these per time a video is already in the queue - OP's idea, so a tap is never in doubt. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <size
        android:width="7dp"
        android:height="7dp" />
    <solid android:color="@color/accent" />
</shape>
